# /review — 変更レビュー用カスタムコマンド

カレントの差分を、次の観点でレビューしてください。指摘は重要度順に、根拠と修正案つきで。

1. バグ・ロジックエラー（境界値・null/undefined・例外パス）
2. セキュリティ（入力検証・シークレット混入・インジェクション）
3. 既存スタイル/規約との不一致（.cursor/rules を基準に）
4. 重複・過剰な複雑さ（既存関数で代替できないか）
5. テストの不足（追加すべきケース）

推測で「問題なし」と言わず、確認した範囲を明示してください。
重大な問題が無い場合はその旨を述べてください。
